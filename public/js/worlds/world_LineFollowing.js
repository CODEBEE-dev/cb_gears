i18n.append({
  '#lineFollowing-shortDescription#': {
    en: 'Line Following Challenges',
    ko: '라인 트레이싱 도전',
  },
  '#lineFollowing-longDescription#': {
    en: '<p>A series of challenges relating to line following.</p>' +
        '<p>Meta challenge: Write a single program that can complete all of the challenges except "Junctions 1".</p>',
    ko: '<p>라인 트레이싱과 관련된 일련의 도전입니다.</p>' +
        '<p>메타 도전: "교차로 1"을 제외한 모든 도전을 완료할 수 있는 단일 프로그램을 작성하세요.</p>',
  },
  '#lineFollowing-selectChallenge#': {
    en: 'Select Challenge',
    ko: '도전 선택',
  },
  '#lineFollowing-simpleCurves#': {
    en: 'Simple Curves',
    ko: '단순 곡선',
  },
  '#lineFollowing-sharpTurns#': {
    en: 'Sharp Turns',
    ko: '급격한 회전',
  },
  '#lineFollowing-gaps1#': {
    en: 'Gaps 1',
    ko: '갭 1',
  },
  '#lineFollowing-gaps2#': {
    en: 'Gaps 2',
    ko: '갭 2',
  },
  '#lineFollowing-obstacles1#': {
    en: 'Obstacles 1',
    ko: '장애물 1',
  },
  '#lineFollowing-obstacles2#': {
    en: 'Obstacles 2',
    ko: '장애물 2',
  },
  '#lineFollowing-obstacles3#': {
    en: 'Obstacles 3',
    ko: '장애물 3',
  },
  '#lineFollowing-obstacles4#': {
    en: 'Obstacles 4',
    ko: '장애물 4',
  },
  '#lineFollowing-junctions1#': {
    en: 'Junctions 1',
    ko: '교차로 1',
  },
  '#lineFollowing-junctions2#': {
    en: 'Junctions 2',
    ko: '교차로 2',
  },
  '#lineFollowing-simpleHTML#': {
    en: '<p class="bold">A simple line following map.</p>' +
        '<p>You should be able to complete this using either a single sensor or double sensor line follower robot.<p>',
    ko: '<p class="bold">단순한 라인 트레이싱 지도입니다.</p>' +
        '<p>단일 센서 또는 이중 센서 라인 트레이싱 로봇으로 완료할 수 있습니다.<p>',
  },
  '#lineFollowing-sharpHTML#': {
    en: '<p class="bold">Line following map with sharper turns.</p>' +
        '<p>It can be hard to make all the sharp turns while maintaining a smooth movement on the straights.<p>',
    ko: '<p class="bold">급격한 회전이 포함된 라인 트레이싱 지도입니다.</p>' +
        '<p>직선 구간에서 부드러운 움직임을 유지하면서 모든 급격한 회전을 하기가 어려울 수 있습니다.<p>',
  },
  '#lineFollowing-gaps1HTML#': {
    en: '<p class="bold">Line following map with gaps.</p>' +
        '<p>You\'ll need a double sensor line follower robot to cross these gaps.<p>',
    ko: '<p class="bold">갭이 있는 라인 트레이싱 지도입니다.</p>' +
        '<p>갭을 건너려면 이중 센서 라인 트레이싱 로봇이 필요합니다.<p>',
  },
  '#lineFollowing-gaps2HTML#': {
    en: '<p class="bold">Line following map with gaps (hard).</p>' +
        '<p>These gaps are wider and have only a short straight section between them.<p>',
    ko: '<p class="bold">갭이 있는 라인 트레이싱 지도 (어려움)입니다.</p>' +
        '<p>갭이 더 넓고, 갭 사이의 직선 구간이 매우 짧습니다.<p>',
  },
  '#lineFollowing-obstacles1HTML#': {
    en: '<p class="bold">Line following map with obstacles.</p>' +
        '<p>The obstacles are immovable and are 20cm x 20cm. ' +
        'You\'ll need to leave the line to navigate around them.<p>',
    ko: '<p class="bold">장애물이 있는 라인 트레이싱 지도입니다.</p>' +
        '<p>장애물은 이동할 수 없으며 20cm x 20cm 크기입니다. ' +
        '장애물을 피해 이동하려면 라인을 벗어나야 합니다.<p>',
  },
  '#lineFollowing-obstacles2HTML#': {
    en: '<p class="bold">Obstacles and blocked path.</p>' +
        '<p>Sometimes, there\'s only one way to navigate around the obstacle.</p>' +
        '<p class="bold">This world randomizes on reset!<p>',
    ko: '<p class="bold">장애물과 막힌 경로입니다.</p>' +
        '<p>때로는 장애물을 피하는 방법이 하나뿐입니다.</p>' +
        '<p class="bold">이 맵은 초기화 시 무작위로 변경됩니다!<p>',
  },
  '#lineFollowing-obstacles3HTML#': {
    en: '<p class="bold">Obstacles and world edge.</p>' +
        '<p>Obstacles are not the only things that can block your path.</p>' +
        '<p class="bold">This world randomizes on reset!<p>',
    ko: '<p class="bold">장애물과 맵 경계입니다.</p>' +
        '<p>경로를 막는 것은 장애물뿐만이 아닙니다.</p>' +
        '<p class="bold">이 맵은 초기화 시 무작위로 변경됩니다!<p>',
  },
  '#lineFollowing-obstacles4HTML#': {
    en: '<p class="bold">Different obstacles and lines.</p>' +
        '<p>Obstacles are not always the same size, the exit point isn\'t always opposite of the entry point either.</p>' +
        '<p>Hint: The maze runner robot can be useful for this world, but isn\'t absolutely necessary.</p>' +
        '<p class="bold">The obstacle sizes randomizes on reset!<p>',
    ko: '<p class="bold">다양한 장애물과 라인입니다.</p>' +
        '<p>장애물의 크기가 항상 동일하지 않으며, 출구 지점이 항상 입구 지점의 반대편에 있지도 않습니다.</p>' +
        '<p>힌트: 미로 탐색 로봇이 이 맵에 유용할 수 있지만, 반드시 필요한 것은 아닙니다.</p>' +
        '<p class="bold">장애물 크기는 초기화 시 무작위로 변경됩니다!<p>',
  },
  '#lineFollowing-junctions1HTML#': {
    en: '<p class="bold">Handling junctions.</p>' +
        '<p>Junctions are not difficult to handle, but can be tedious to program for unless you have prepared suitable functions.</p>' +
        '<p>Task 1: Pick a random point (eg. "D") and program your robot to go there and stop.</p>' +
        '<p>Task 2: Pick a series of random points (eg. "C, L, A, Q, J,") and program your robot to go to each end point in turn. ' +
        'Beep at each point and stop at the last.</p>' +
        '<p>You\'ll need a double sensor line follower robot for this world.</p>',
    ko: '<p class="bold">교차로 처리입니다.</p>' +
        '<p>교차로 처리는 어렵지 않지만, 적절한 함수를 미리 준비하지 않으면 프로그래밍이 번거로울 수 있습니다.</p>' +
        '<p>과제 1: 임의의 지점(예: "D")을 선택하고 로봇이 그곳으로 이동하여 정지하도록 프로그래밍하세요.</p>' +
        '<p>과제 2: 일련의 임의 지점(예: "C, L, A, Q, J")을 선택하고 로봇이 각 종점을 순서대로 방문하도록 프로그래밍하세요. ' +
        '각 지점에서 비프음을 내고 마지막 지점에서 정지하세요.</p>' +
        '<p>이 맵에는 이중 센서 라인 트레이싱 로봇이 필요합니다.</p>',
  },
  '#lineFollowing-junctions2HTML#': {
    en: '<p class="bold">Turn Indicators.</p>' +
        '<p>At each junction, turn in the direction marked by the green indicator. ' +
        'Load the world to see an illustrated guide.</p>' +
        '<p>Follow the indicated turns and get to the end point.</p>' +
        '<p>You\'ll need a double sensor line follower robot for this world.</p>',
    ko: '<p class="bold">방향 표시기입니다.</p>' +
        '<p>각 교차로에서 초록색 표시기가 가리키는 방향으로 회전하세요. ' +
        '맵을 불러오면 삽화 안내를 확인할 수 있습니다.</p>' +
        '<p>표시된 방향대로 회전하며 종점까지 이동하세요.</p>' +
        '<p>이 맵에는 이중 센서 라인 트레이싱 로봇이 필요합니다.</p>',
  },
  '#lineFollowing-startPosXY#': {
    en: 'Starting Position (x, y)',
    ko: '시작 위치 (x, y)',
  },
  '#lineFollowing-startPosXYHelp#': {
    en: 'Enter using this format "x, y" (in cm, without quotes) and it will override the above. Center of image is "0, 0".',
    ko: '"x, y" 형식으로 입력하면 (cm 단위, 따옴표 제외) 위 설정을 덮어씁니다. 이미지 중앙이 "0, 0"입니다.',
  },
  '#lineFollowing-startRot#': {
    en: 'Starting Rotation (degrees)',
    ko: '시작 방향 (도)',
  },
  '#lineFollowing-startRotHelp#': {
    en: 'Set the starting rotation in degrees. Positive rotation is clockwise.',
    ko: '시작 방향을 도(°) 단위로 설정합니다. 양수 값은 시계 방향입니다.',
  },
});

var world_LineFollowing = new function() {
  var self = this;

  this.name = 'lineFollowing';
  this.shortDescription = i18n.get('#lineFollowing-shortDescription#');
  this.longDescription = i18n.get('#lineFollowing-longDescription#');
  this.thumbnail = 'images/worlds/lineFollowing.jpg';

  this.options = {};
  this.robotStart = {
    position: new BABYLON.Vector3(0, 0, 0),
    rotation: new BABYLON.Vector3(0, 0, 0)
  };

  this.optionsConfigurations = [
    {
      option: 'image',
      title: i18n.get('#lineFollowing-selectChallenge#'),
      type: 'selectWithHTML',
      options: [
        [i18n.get('#lineFollowing-simpleCurves#'), 'simple'],
        [i18n.get('#lineFollowing-sharpTurns#'), 'sharp'],
        [i18n.get('#lineFollowing-gaps1#'), 'gaps1'],
        [i18n.get('#lineFollowing-gaps2#'), 'gaps2'],
        [i18n.get('#lineFollowing-obstacles1#'), 'obstacles1'],
        [i18n.get('#lineFollowing-obstacles2#'), 'obstacles2'],
        [i18n.get('#lineFollowing-obstacles3#'), 'obstacles3'],
        [i18n.get('#lineFollowing-obstacles4#'), 'obstacles4'],
        [i18n.get('#lineFollowing-junctions1#'), 'junctions1'],
        [i18n.get('#lineFollowing-junctions2#'), 'junctions2'],
      ],
      optionsHTML: {
        simple:     i18n.get('#lineFollowing-simpleHTML#'),
        sharp:      i18n.get('#lineFollowing-sharpHTML#'),
        gaps1:      i18n.get('#lineFollowing-gaps1HTML#'),
        gaps2:      i18n.get('#lineFollowing-gaps2HTML#'),
        obstacles1: i18n.get('#lineFollowing-obstacles1HTML#'),
        obstacles2: i18n.get('#lineFollowing-obstacles2HTML#'),
        obstacles3: i18n.get('#lineFollowing-obstacles3HTML#'),
        obstacles4: i18n.get('#lineFollowing-obstacles4HTML#'),
        junctions1: i18n.get('#lineFollowing-junctions1HTML#'),
        junctions2: i18n.get('#lineFollowing-junctions2HTML#'),
      }
    },
    {
      option: 'startPosXY',
      title: i18n.get('#lineFollowing-startPosXY#'),
      type: 'text',
      help: i18n.get('#lineFollowing-startPosXYHelp#')
    },
    {
      option: 'startRot',
      title: i18n.get('#lineFollowing-startRot#'),
      type: 'text',
      help: i18n.get('#lineFollowing-startRotHelp#')
    }
  ];

  this.imagesURL = {
    simple: 'textures/maps/Line Following/Simple.png',
    sharp: 'textures/maps/Line Following/Sharp.png',
    gaps1: 'textures/maps/Line Following/Gaps 1.png',
    gaps2: 'textures/maps/Line Following/Gaps 2.png',
    obstacles1: 'textures/maps/Line Following/Obstacles 1.png',
    obstacles2: 'textures/maps/Line Following/Obstacles 2.png',
    obstacles3: null,
    obstacles4: 'textures/maps/Line Following/Obstacles 4.png',
    junctions1: 'textures/maps/Line Following/Junctions 1.png',
    junctions2: 'textures/maps/Line Following/Junctions 2.png',
  };

  this.robotStarts = {
    simple: new BABYLON.Vector3(0, 0, -91),
    sharp: new BABYLON.Vector3(15, 0, -91),
    gaps1: new BABYLON.Vector3(0, 0, -91),
    gaps2: new BABYLON.Vector3(15, 0, -91),
    obstacles1: new BABYLON.Vector3(0, 0, -91),
    obstacles2: new BABYLON.Vector3(0, 0, -141),
    obstacles3: new BABYLON.Vector3(0, 0, -141),
    obstacles4: new BABYLON.Vector3(75, 0, -91),
    junctions1: new BABYLON.Vector3(0, 0, -6),
    junctions2: new BABYLON.Vector3(0, 0, -91),
  }

  this.defaultOptions = {
    image: 'simple',
    length: 100,
    width: 100,
    groundFriction: 1,
    wallFriction: 0.1,
    groundRestitution: 0.0,
    wallRestitution: 0.1,
    startPosXY: '',
    startRot: ''
  };

  // Set options, including default
  this.setOptions = function(options) {
    Object.assign(self.options, self.defaultOptions);

    for (let name in options) {
      if (typeof self.options[name] == 'undefined') {
        console.log('Unrecognized option: ' + name);
      } else {
        self.options[name] = options[name];
      }
    }

    self.robotStart.position = self.robotStarts[self.options.image];

    if (typeof self.options.startPosXY != 'undefined' && self.options.startPosXY.trim() != '') {
      let xy = self.options.startPosXY.split(',');
      self.robotStart.position = new BABYLON.Vector3(parseFloat(xy[0]), 0, parseFloat(xy[1]));
    }
    if (typeof self.options.startRot != 'undefined' && self.options.startRot.trim() != '') {
      self.robotStart.rotation.y = parseFloat(self.options.startRot) / 180 * Math.PI;
    }

    return new Promise(function(resolve, reject) {
      if (! self.imagesURL[self.options.image]) {
        resolve();
        return;
      }

      var img = new Image();
      img.crossOrigin = "anonymous";
      img.onload = function() {
        self.options.length = this.width / 10.0;
        self.options.width = this.height / 10.0;

        resolve();
      }
      img.src = self.imagesURL[self.options.image];
    });
  };

  // Run on page load
  this.init = function() {
    self.setOptions();
  };

  // Load image into ground tile
  this.loadImageTile = function (scene, imageSrc, size, pos=[0,0], rot=Math.PI/2) {
    var groundMat = new BABYLON.StandardMaterial('ground', scene);
    var groundTexture = new BABYLON.Texture(imageSrc, scene);
    groundMat.diffuseTexture = groundTexture;
    groundMat.diffuseColor = new BABYLON.Color3(1, 1, 1);
    groundMat.specularColor = new BABYLON.Color3(0.1, 0.1, 0.1);

    var faceUV = new Array(6);
    for (var i = 0; i < 6; i++) {
        faceUV[i] = new BABYLON.Vector4(0, 0, 0, 0);
    }
    faceUV[4] = new BABYLON.Vector4(0, 0, 1, 1);

    var boxOptions = {
        width: size[0],
        height: 10,
        depth: size[1],
        faceUV: faceUV
    };

    var ground = BABYLON.MeshBuilder.CreateBox('box', boxOptions, scene);
    ground.material = groundMat;
    ground.receiveShadows = true;
    ground.position.y = -5;
    ground.position.x = pos[0];
    ground.position.z = pos[1];
    ground.rotation.y = rot;

    // Physics
    ground.physicsImpostor = new BABYLON.PhysicsImpostor(
      ground,
      BABYLON.PhysicsImpostor.BoxImpostor,
      {
        mass: 0,
        friction: self.options.groundFriction,
        restitution: self.options.groundRestitution
      },
      scene
    );
  };

  // Obstacles 2
  this.loadObstacles2 = function(scene) {
    self.loadImageTile(
      scene,
      self.imagesURL[self.options.image],
      [self.options.width, self.options.length]
    );

    self.addBox(scene, [20,20,20], [0, -98]);
    if (Math.random() > 0.5) {
      self.addBox(scene, [20,30,20], [30, -123]);
    } else {
      self.addBox(scene, [20,30,20], [-30, -123]);
    }

    self.addBox(scene, [20,20,20], [0, -2]);
    if (Math.random() > 0.5) {
      self.addBox(scene, [20,30,20], [30, -2]);
    } else {
      self.addBox(scene, [20,30,20], [-30, -2]);
    }

    self.addBox(scene, [20,20,20], [0, 90]);
    if (Math.random() > 0.5) {
      self.addBox(scene, [20,30,20], [30, 115]);
    } else {
      self.addBox(scene, [20,30,20], [-30, 115]);
    }
  };

  // Obstacles 3
  this.loadObstacles3 = function(scene) {
    function loadCenter(y, length) {
      self.loadImageTile(
        scene,
        'textures/maps/Line Following/Obstacles 3 Center.png',
        [length, 100],
        [0, y]
      );
    }

    function loadLeftRight(y, length) {
      let posAndRot = [];
      if (Math.random() > 0.5) {
        posAndRot = [35, Math.PI/2]
      } else {
        posAndRot = [-35, -Math.PI/2]
      }
      self.loadImageTile(
        scene,
        'textures/maps/Line Following/Obstacles 3 Left.png',
        [length, 100],
        [posAndRot[0], y],
        posAndRot[1]
      );
    }

    loadCenter(-135, 30);
    loadLeftRight(-110, 20);
    self.addBox(scene, [20,20,20], [0, -110]);

    loadCenter(-70, 60);
    loadLeftRight(-30, 20);
    loadCenter(45, 130);
    self.addBox(scene, [20,20,20], [0, -10]);

    loadLeftRight(155, 90);
    self.addBox(scene, [20,20,20], [0, 100]);
  };

  // Obstacles 4
  this.loadObstacles4 = function(scene) {
    function randomSizedBox(x, y) {
      let width = Math.random() * 30 + 10;
      let depth = Math.random() * 30 + 10;

      self.addBox(scene, [20,width,depth], [x, y]);
    }

    self.loadImageTile(
      scene,
      self.imagesURL[self.options.image],
      [self.options.width, self.options.length]
    );

    randomSizedBox(75.0, -36.9);
    randomSizedBox(-68.9, -36.9);
    randomSizedBox(-68.9, 74.6);
    randomSizedBox(-9.6, 15.3);
    randomSizedBox(73.1, 15.3);
  };

  // Create the scene
  this.load = function (scene) {
    return new Promise(function(resolve, reject) {
      self.obstacleMat = babylon.getMaterial(scene, 'CC1ACC')

      if (self.options.image == 'obstacles1') {
        self.loadImageTile(
          scene,
          self.imagesURL[self.options.image],
          [self.options.width, self.options.length]
        );

        self.addBox(scene, [20,20,20], [0, -48]);
        self.addBox(scene, [20,20,20], [0, 48]);
      } else if (self.options.image == 'obstacles2') {
        self.loadObstacles2(scene);
      } else if (self.options.image == 'obstacles3') {
        self.loadObstacles3(scene);
      } else if (self.options.image == 'obstacles4') {
        self.loadObstacles4(scene);
      } else {

        self.loadImageTile(
          scene,
          self.imagesURL[self.options.image],
          [self.options.width, self.options.length]
        );
      }
      resolve();
    });
  };

  // Add static box
  this.addBox = function(scene, size, pos) {
    var boxOptions = {
      height: size[0],
      width: size[1],
      depth: size[2]
    };

    var box = BABYLON.MeshBuilder.CreateBox('obstacle', boxOptions, scene);
    box.material = self.obstacleMat;
    box.position.y = size[0] / 2;
    box.position.x = pos[0];
    box.position.z = pos[1];

    box.physicsImpostor = new BABYLON.PhysicsImpostor(
      box,
      BABYLON.PhysicsImpostor.BoxImpostor,
      {
        mass: 0,
        friction: self.options.wallFriction,
        restitution: self.options.wallRestitution
      },
      scene
    );
  };
}

// Init class
world_LineFollowing.init();

if (typeof worlds == 'undefined') {
  var worlds = [];
}
worlds.push(world_LineFollowing);
